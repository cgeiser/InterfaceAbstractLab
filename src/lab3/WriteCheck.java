package lab3;

/**In this example, only used in "Checking", but could be reused elsewhere.
 *
 * @author chrisgeiser
 */
public interface WriteCheck {
    
        public void writeCheck(int checkNumber, double checkAmount);
        
        public double getCheckAmount(int checkNumber);
    
}
